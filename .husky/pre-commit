#!/usr/bin/env sh

npm run build || {
	echo "\nBuild failed — aborting commit. Fix build errors and try again."
	exit 1
}

if [ -n "$(git status --porcelain docs)" ]; then
	git add -A docs
	git commit --no-verify -m "chore: build" || true
fi

